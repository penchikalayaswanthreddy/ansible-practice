- name: Functions in ansible
  hosts: local
  connection: local
  become: yes
  tasks: 
  - name: execute a command with shell module
    ansible.builtin.command: 
      ls -l
    register: command_output
  - name: print the shell command_output
    ansible.builtin.debug:
      msg: " {{ command_output }}"

