- name: Functions in ansible
  hosts: local
  connection: local
  become: yes
  vars: 
    fruits: 
    - Apple
    - Banana
    - carrot
    - Mango
  tasks: 
  - name : Printing the varaible
    ansible.builtin.debug: 
      msg: " {{ fruits | items2dict }}"