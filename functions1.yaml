- name: Functions in ansible
  hosts: local
  connection: local
  become: yes
  vars: 
    employee: yaswanth, muni, mukund, ganga
  tasks: 
  - name : Printing the varaible
    ansible.builtin.debug: 
      msg: " {{ employee | split(',') }}"