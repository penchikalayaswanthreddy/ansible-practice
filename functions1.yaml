- name: Functions in ansible
  hosts: local
  connection: local
  become: yes
  vars: 
    Address:
      DoorNo: 14/98
      Nagar: kamalanagar
  tasks: 
  - name : Printing the varaible
    ansible.builtin.debug: 
      msg: " {{ Address | dict2items }}"